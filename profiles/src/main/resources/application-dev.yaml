# dev profile
spring:
  profiles:
    active: dev
  datasource:
    url: jdbc:h2:mem:devdb
    username: sa
    password: password
app:
  api-key: "DEV-KEY-123"
  features:
    - dashboard
    - users-management
  logging:
    level: DEBUG
